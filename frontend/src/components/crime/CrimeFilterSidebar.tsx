import type { CrimeFilters } from "../../types/crime";
import { crimeStatuses, crimeTypes, labelize, priorities } from "../../types/crime";
export function CrimeFilterSidebar({ filters, onChange, onClear }: { filters: CrimeFilters; onChange: (filters: CrimeFilters) => void; onClear: () => void }) { const set = (key: keyof CrimeFilters, value: string) => onChange({ ...filters, [key]: value }); return <aside className="filter-sidebar"><div><h2>Filter reports</h2><button onClick={onClear}>Clear</button></div><label>Crime type<select value={filters.crime_type || ""} onChange={(e) => set("crime_type", e.target.value)}><option value="">All types</option>{crimeTypes.map((value) => <option value={value} key={value}>{labelize(value)}</option>)}</select></label><label>Status<select value={filters.status || ""} onChange={(e) => set("status", e.target.value)}><option value="">All statuses</option>{crimeStatuses.map((value) => <option value={value} key={value}>{labelize(value)}</option>)}</select></label><label>Priority<select value={filters.priority || ""} onChange={(e) => set("priority", e.target.value)}><option value="">All priorities</option>{priorities.map((value) => <option value={value} key={value}>{labelize(value)}</option>)}</select></label><label>District<input value={filters.district || ""} onChange={(e) => set("district", e.target.value)} /></label><label>Incident date<input type="date" value={filters.incident_date || ""} onChange={(e) => set("incident_date", e.target.value)} /></label></aside>; }
