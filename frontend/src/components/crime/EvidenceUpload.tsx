import { FileUp, X } from "lucide-react";
interface Props { files: File[]; onChange: (files: File[]) => void; }
const supported = ["image/jpeg", "image/png", "image/webp", "video/mp4", "video/webm", "application/pdf"];
const maxBytes = 25 * 1024 * 1024;
export function EvidenceUpload({ files, onChange }: Props) { const selectFiles = (selected: FileList | null) => { if (!selected) return; const accepted = Array.from(selected).filter((file) => supported.includes(file.type) && file.size <= maxBytes); onChange([...files, ...accepted]); }; return <div className="evidence-upload"><label className="upload-zone"><FileUp size={24} /><span>Add evidence</span><small>JPEG, PNG, WEBP, MP4, WEBM or PDF · 25 MB each</small><input type="file" multiple accept="image/jpeg,image/png,image/webp,video/mp4,video/webm,application/pdf" onChange={(event) => selectFiles(event.target.files)} /></label>{files.length > 0 && <ul className="file-list">{files.map((file, index) => <li key={`${file.name}-${index}`}><span>{file.name} <small>({(file.size / 1024 / 1024).toFixed(1)} MB)</small></span><button type="button" onClick={() => onChange(files.filter((_, current) => current !== index))} aria-label={`Remove ${file.name}`}><X size={16} /></button></li>)}</ul>}</div>; }
